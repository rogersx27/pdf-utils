# FastAPI Configuration

# API Metadata
API_TITLE=PDF Analyzer API
API_DESCRIPTION=REST API for analyzing Colombian bank statement PDFs
API_VERSION=1.0.0

# Server Settings
HOST=0.0.0.0
PORT=8000
RELOAD=true
LOG_LEVEL=info

# Environment
ENVIRONMENT=development

# CORS (comma-separated origins, or use * for all)
CORS_ORIGINS=*

# PDF Password (inherited from parent project)
PDF_PASSWORD=
